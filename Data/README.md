## NOTE ABOUT DATASET LOCATION

The gzipped JSON and CSV versions of our original_dataset and cleaned_dataset were a total of 5.5 GB, and we couldn't upload through Git.

We have uploaded them to a folder in the `ESSD AI Competition` Teams channel, in the `General` chat.

The folder path is: `Documents/General/THE PHILADELPHIA EAGLES/week_2`.

I've provided a screenshot in the repository, showing an example of where the files are stored.
